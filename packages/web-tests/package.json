{
  "name": "web-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:test": "playwright test",
    "start-server": "cd ../../ && pnpm dev",
    "e2e:test": "start-server-and-test start-server http://localhost:3000 dev:test"
  },
  "dependencies": {
    "start-server-and-test": "^1.14.0",
    "app-template": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.29.1",
    "tsconfig": "workspace:*",
    "typescript": "^4.9.3"
  }
}
