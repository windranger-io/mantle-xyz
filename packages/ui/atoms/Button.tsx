import * as React from 'react'

/**
 * The most amazing button ever made
 *
 */
export const Button = () => (
  <div onClick={() => 0} onKeyDown={() => ''} role="button" tabIndex={0}>
    Click Me
  </div>
)
